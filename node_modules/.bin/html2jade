#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../html2jade/bin/html2jade" "$@"
  ret=$?
else 
  node  "$basedir/../html2jade/bin/html2jade" "$@"
  ret=$?
fi
exit $ret
